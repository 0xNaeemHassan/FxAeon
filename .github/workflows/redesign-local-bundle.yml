name: Redesign local bundle

on:
  push:
    branches: [feat/unified-product-redesign]
    paths: ['.github/workflows/redesign-local-bundle.yml']

permissions:
  contents: read

jobs:
  bundle:
    if: github.repository == '0xNaeemHassan/FxAeon' && github.ref == 'refs/heads/feat/unified-product-redesign'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@a15d269cd4658e1107c09f1fabf4cbd7bd1f308a
        with:
          version: 11.19.0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '22'
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Export source without Git credentials
        run: |
          git archive --format=tar.gz --output="$RUNNER_TEMP/fxaeon-source.tar.gz" HEAD
          git rev-parse HEAD > "$RUNNER_TEMP/source-revision.txt"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: redesign-source
          path: |
            ${{ runner.temp }}/fxaeon-source.tar.gz
            ${{ runner.temp }}/source-revision.txt
          retention-days: 1
          compression-level: 0
      - name: Prepare Chromium for the isolated visual checks
        run: |
          pnpm --dir apps/mini-app exec playwright install chromium
          tar -czf "$RUNNER_TEMP/fxaeon-browser.tar.gz" -C "$HOME/.cache" ms-playwright
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: redesign-browser
          path: ${{ runner.temp }}/fxaeon-browser.tar.gz
          retention-days: 1
          compression-level: 0
