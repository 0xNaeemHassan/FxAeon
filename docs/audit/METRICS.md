# Archived metrics proposal

The original metrics document mixed planned instrumentation with implemented behavior and therefore cannot serve as a current SLO contract.

Current probes, worker coverage, logging, backups, and routine checks are documented in [Operations](../operations.md). Any formal SLO must name a live measurement source, alert owner, evaluation window, and verified production dashboard before being presented as enforced.
